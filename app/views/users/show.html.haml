.row.main
	.row
		%h1 Profile: #{current_user.email}
	.row

		%dl.accordion{"data-accordion" => ""}
			-current_user.work_groups.each do |group|
				%dd
					%a{href: "#panel#{group.id}"} Accordion 1
					#panel1.content.active
						%dl.tabs{"data-tab" => ""}
							-group.works.each do |work|
								%dd.active
									%a{href: "#panel#{group.id}-#{work.id}"} #{work.title}
						.tabs-content
							-group.works.each do |work|
								#panel{class:"#{group.id}-#{work.id}"}
									%p First panel content goes here...