= simple_form_for @work_group, remote: true do |f|
	= f.error_notification

	.form-inputs
		= f.input :name

	=f.hidden_field :user_id, :value => @user.id
	
	.actions
		= f.submit nil, :class => 'button tiny'


	#spinner.spinner
		= image_tag "ajax-loader.gif"
	#response